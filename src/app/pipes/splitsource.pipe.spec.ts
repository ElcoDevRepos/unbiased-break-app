import { SplitsourcePipe } from './splitsource.pipe';

describe('SplitsourcePipe', () => {
  it('create an instance', () => {
    const pipe = new SplitsourcePipe();
    expect(pipe).toBeTruthy();
  });
});
