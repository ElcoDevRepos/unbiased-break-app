import { GetRealSitePipe } from './get-real-site.pipe';

describe('GetRealSitePipe', () => {
  it('create an instance', () => {
    const pipe = new GetRealSitePipe();
    expect(pipe).toBeTruthy();
  });
});
