import { ActiveTopicPipe } from './active-topic.pipe';

describe('ActiveTopicPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveTopicPipe();
    expect(pipe).toBeTruthy();
  });
});
